[project]
name = "ferrocene-sphinx-shared-resources"
version = "0.0.0"
requires-python = ">=3.10"
dependencies = ["sphinx", "sphinx-needs", "tomli"]

[tool.hatch.build]
only-include = ["src"]
sources = ["src"]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
